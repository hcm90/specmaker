<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <!-- Header and Nav -->  
  <div class="row">
    <div class="large-3 columns" >
      <h1><%=link_to image_tag("/mekal.png"), new_specsheet_path %></h1>
    </div>
    <div class="large-9 columns">
      <ul class="inline-list right">
        <% if current_user %>
          <li>Signed in as <%= link_to current_user.email, user_path(current_user) %></li>
          <li><%= link_to "Log out", session_path("current"), :method => :delete %></li>
        <% else %>
          <li><%= link_to "Log in", new_session_path %></li>
          <li><%= link_to "Sign up", new_user_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
  
  <!-- End Header and Nav -->
  
  
  <div class="row">    
    
    <!-- Main Content Section -->
    <!-- This has been source ordered to come first in the markup (and on small devices) but to be to the right of the nav on larger screens -->
    <div class="large-9 push-1 columns">
      
      <%= yield %>
            
    </div>
    
  </div>
<!-- 
  <section>
       <div class="frame">
           <div class="col1 main-content">
              <% if flash[:notice] %>
                <p id="notice"><%= flash[:notice] %></p>
              <% end %>
              <% if flash[:alert] %>
                <p id="alert"><%= flash[:alert] %></p>
              <% end %>
           </div>
       </div>
   </section> -->
  
  
  <!-- Footer -->
<!--   
  <footer class="row">
    <div class="large-12 columns">
      <hr />
      <div class="row">
        <div class="large-12 columns">
          <p>© Copyright helloheathergraham, itsheitor!</p>
        </div>
      </div>
    </div> 
  </footer> -->
    <%= javascript_include_tag "application" %>
  </body>
</html>